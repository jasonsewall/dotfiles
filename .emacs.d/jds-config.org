#+Title: Jason Sewall's Emacs config
#+OPTIONS: toc:4 h:4
#+PROPERTY: header-args:emacs-lisp :tangle init.el


* Cheatsheet — trying out the modern stack

A running guide. Keep updated as packages change. Nothing in this
section tangles (no source blocks); it's documentation.

** Completion: minibuffer (vertico stack)

Vertico is the vertical minibuffer UI; marginalia adds annotations;
orderless lets you type out-of-order substrings (space separator);
consult adds richer commands; embark acts on whatever you've selected.

- =M-x= , =C-x b= , =C-x C-f= — already vertico-powered; just type
  partial words in any order
- =C-x b= (=consult-buffer=) — switch buffer with live preview
- =C-x C-r= (=consult-recent-file=) — recent files
- =M-s f= or =M-g g= (=consult-line=) — fuzzy-search current buffer
- =M-g r= (=consult-ripgrep=) — ripgrep across the project
- =M-g i= (=consult-imenu=) — jump to function/heading by name
- =M-g o= (=consult-outline=) — outline navigation
- =M-y= (=consult-yank-pop=) — visual kill-ring
- =C-c o= or =C-.= (=embark-act=) — act on target at point
  (file → dired/copy/attach; buffer → kill/rename; URL → browse…)
- =M-o= inside the minibuffer — =embark-act= on the current candidate
- =C-x M-r= (=vertico-repeat=) — re-run last completion session
- type =!foo= in minibuffer — exclude matches containing "foo"
  (orderless dispatcher); =foo== — match "foo" literally

** Completion: in-buffer (corfu + cape)

Corfu now =:custom corfu-auto t= so the popup triggers as you type.
Pause to see =corfu-popupinfo= docs. =cape= adds dabbrev/file/keyword
sources to =completion-at-point-functions=.

- (just type) — corfu popup; arrows / =C-n=/=C-p= to navigate; =RET=
  accepts; =TAB= cycles
- =C-M-i= (=completion-at-point=) — force completion (file paths,
  dabbrev expansions, etc., from cape)
- =M-/= (=hippie-expand=) — old-school stepwise expansion fallback

** LSP / diagnostics (eglot + flymake + eldoc-box)

- visit a code buffer in a configured mode (C/C++/CUDA/Rust/Python) —
  eglot starts automatically via the per-mode hooks
- =M-.= / =M-?= — jump to definition / find references
- =M-x eglot-rename= — rename across the project
- =M-x eglot-code-actions= — quick fixes / refactors
- =M-n= / =M-p= — next/previous flymake diagnostic
- =C-c != — echo flymake diagnostic(s) for the *current line*
  (just put point anywhere on the line — no need to be inside the
  underlined span)
- =C-c ! l= — flymake buffer diagnostics list
- when point is on a line with diagnostics, the message also shows
  in =eldoc-box= automatically (we replaced flymake's point-only
  eldoc reporter with a line-aware one — see
  =jds/flymake-eldoc-on-line=)
- =C-h .= — eldoc-box: float a child frame with signature/docs
  (instead of cramped echo area); auto-on in eglot buffers
- rustic buffers also have =C-c C-c a/r/q/Q= for code-actions/rename/
  reconnect/shutdown, and =C-c C-c l= for diagnostics

** Python (uv-first)

Prerequisites on =$PATH= (install via uv or pipx):

#+begin_example
uv tool install basedpyright   # LSP (types, hover, rename)
uv tool install ruff           # format + lint
uv tool install mypy           # second type checker
uv tool install ty             # third type checker (Astral, alpha)
#+end_example

In a project:

- =uv venv= creates =.venv/= — that's all =pet= needs to wire up
- open any =.py= file:
  - =pet-mode= picks up =.venv/= and points pyvenv, eglot, mypy, ty
    at the right interpreter
  - =eglot= starts with basedpyright → hover docs, completion,
    rename, code actions
  - =flymake= shows three streams of diagnostics inline:
    basedpyright (LSP) + mypy (=flymake-mypy=) + ty
    (=jds/flymake-ty-backend=); each ty diagnostic is tagged =[ty]=
  - =apheleia= runs =ruff format= on save
- =M-x pet-verify-setup= — diagnose which tools/venv pet found
- =M-x jds/python-check-tools= — list any missing CLI tools with
  =uv tool install ...= hints. Auto-runs the first time a Python
  buffer opens this session; pops the =*python-tools*= buffer if
  anything's missing. =C-u M-x jds/python-check-tools= forces a
  re-check after you've installed things.
- the modeline shows =venv:NAME= when a venv is active

** Tree-sitter (treesit-auto)

- visit any =.c=/=.rs=/=.go=/etc. file — you'll be prompted once to
  install the grammar, then the buffer uses the =-ts-mode= variant
- =M-x treesit-auto-install-all= — pre-install everything

** Markdown / TOML / YAML

Tree-sitter modes for the structure, eglot for the smarts.
Install the language servers on your =$PATH= (pick whichever
package manager you use):

#+begin_example
# macOS (Homebrew)
brew install pandoc marksman taplo yaml-language-server

# Linux — Debian/Ubuntu
sudo apt install pandoc
# marksman, taplo, yaml-language-server usually not in apt; use:

# Cross-platform fallbacks
cargo install taplo-cli --features lsp
npm install -g yaml-language-server
# marksman: grab a release from
#   https://github.com/artempyanykh/marksman/releases
# and drop it on $PATH (chmod +x it).
#+end_example

- =markdown-mode= (=.md=): pandoc-mode for export, visual-line-mode
  for wrap, native code-block fontification, marksman for hover/refs
- =M-x jds/markdown-reading-mode= — toggle prose-friendly view:
  hides =*=/=_=/=[]()= markup, switches to proportional font (code
  stays monospace), centers a narrow column. Same again to leave.
- =C-c C-c l= — toggle =markdown-live-preview-mode= (renders via
  =pandoc -f gfm -t html5= and shows the result in a side EWW
  buffer that refreshes as you type). Fully local — no server, no
  token, no network.
- =C-c C-c p= — one-shot preview in your default browser
- =C-c C-c v= — export to file + open in browser
- =toml-ts-mode= (=.toml=): taplo gives schema-aware diagnostics
  (=pyproject.toml=, =Cargo.toml=)
- =yaml-ts-mode= (=.yaml=/=.yml=): yaml-language-server validates
  against schemas; pinned to 2-space indent
- =C-c C-c= in pandoc-mode — convert/export via Pandoc

** Projects (built-in project.el — replaces projectile)

The new prefix is =C-x p=. Bindings to remember:

- =C-x p f= — find file in project
- =C-x p g= — grep
- =C-x p r= — query-replace across project
- =C-x p p= — switch project
- =C-x p b= — buffer in project
- =C-x p s= / =C-x p e= — shell / eshell in project
- =C-x p D= — dired the project root
- =C-x p k= — kill all project buffers

** VCS

- =C-x C-g= — =magit-status=
- =C-c m l= — =git-link= (copy URL to current line on the remote)
- =yadm= as a TRAMP method: =C-x C-f /yadm:enter:~/.gitconfig=

** Editing

- =C-x u= (=vundo=) — visualize undo tree; =p/n/f/b= to navigate,
  =q= to exit, =RET= to commit
- =M-2= (=er/expand-region=) — expand selection by syntactic unit
- =C-x m= — multiple-cursors hydra (=l= edit lines, =a= mark all,
  =n/p= mark next/prev, =r= mark all in regex)
  - while multi-cursors are live, =C-s= becomes phi-search (works
    across all cursors)
- =M-3= / =M-4= — quick mark next/prev like this
- =M-SPC= — =cycle-spacing= (toggle 0/1/many spaces)
- =C-c t= — transpose at whitespace
- =C-a= — smart beginning-of-line (toggles indentation vs. column 0)
- =C-x M-q= — unfill paragraph (one line)

** Snippets

- =yas-insert-snippet= or just type a trigger + =TAB=
- snippets live in =~/.emacs.d/snippets/=

** Window / frame nav

- =C-x w= — =hydra-window= (then =b= switch-buffer, =f= find-file,
  =h/j/k/l= move, =v/x= split, =a= ace-jump, =s= swap, =o= delete
  others, =d= delete, =z= winner-undo, =Z= winner-redo)
- =M-g= — =hydra-avy= (=c= char, =C= 2-char, =w= word, =s= subword,
  =u= follow URI)
- =repeat-mode= is on: after =C-x o= (other-window), keep pressing
  =o= to keep cycling. Works for many prefixed commands.

** Workspaces (tab-bar-mode)

Built-in workspaces; tabs are *window configurations*, not buffers.

- =C-x t 2= — new tab
- =C-x t 0= — close current tab
- =C-x t o= — next tab; =C-x t O= — previous
- =C-x t r= — rename tab
- =C-x t b= — switch to buffer in another tab
- =C-x t RET= / =C-x t <N>= — jump to tab by name / number

** Search

- =C-s= / =C-r= — isearch (plain); =TAB= during isearch hands off to
  =consult-line=
- rg.el bindings via =rg-enable-default-bindings= prefix =C-c s=

** Themes

- =M-x ef-themes-select= — browse all variants with live preview
- =M-x ef-themes-toggle= — flip between =ef-dream= ↔ =ef-frost=
- adjust the toggle pair: =(setq ef-themes-to-toggle '(THEME-A THEME-B))=

** Org-mode

- =C-c '= (=org-edit-special=) — edit a src block in its own buffer
- =C-c C-v t= (=org-babel-tangle=) — tangle (also auto-tangle on
  save of this file via =jds/tangle-jds-config-on-save=)
- =org-modern-mode= is on in org buffers — cosmetic
- =org-appear-mode= is on — when point lands on =*bold*=/=/italic/=/
  links, the raw markup briefly reveals so you can edit it

** Lisp REPLs

- =M-x sly= — start a Common Lisp REPL (sbcl); =M-.= jumps, =C-c
  C-k= compile buffer, =C-c C-c= compile defun
- =M-x ielm= — Emacs Lisp REPL (built-in)

** Learning the stack

- =M-x jds/tip= — pop a random tip drawn from THIS cheatsheet. Also
  auto-fires once on Emacs startup (small window; =q= to dismiss).
  Add a bullet here → it's in the rotation next time.

** Misc

- =<f9>= — ibuffer
- =<f10>= — =consult-menu-bar= (search menu commands)
- =C-c g= — goto-line
- =C-x C-c C-v= — actually quit Emacs (=C-x C-c= is unbound to
  prevent butterfinger exits)
- =hydra-desktop/body= — desktop save/restore/clear
- =minions-mode= clicks the modeline-icon to expand minor-mode list
- =breadcrumb-local-mode= shows imenu/project crumbs in the header
  line (auto-on in prog-/text-mode)


* Configuration
** About this file
:PROPERTIES:
:CUSTOM_ID: babel-init
:END:
<<babel-init>>

I decided to re-set my .emacs file after ~13 years. I'm trying to
follow the basic model of http://sachachua.com/dotemacs.

From Sacha:

If you're new to Emacs Lisp, you probably don't want to copy and paste
large chunks of this code. Instead, copy small parts of it (always
making sure to copy a complete set of parentheses) into your
=*scratch*= buffer or some other buffer in =emacs-lisp-mode=. Use =M-x
eval-buffer= to evaluate the code and see if you like the way that
Emacs behaves. See [[https://www.gnu.org/software/emacs/manual/html_mono/eintr.html][An Introduction to Programming in Emacs Lisp]] for
more details on Emacs Lisp. You can also find the manual by using =C-h
i= (=info=) and choosing "Emacs Lisp Intro".

I've installed a lot of packages. See the [[*Add%20package%20sources][package sources]] section to
add the repositories to your configuration. When you see =use-package=
and a package name you might like, you can use =M-x package-install=
to install the package of that name. Note that use-package is itself
provided by a package, so you'll probably want to install that and
=bind-key=.

If you're viewing the Org file, you can open source code blocks (those
are the ones in begin_src) in a separate buffer by moving your point
inside them and typing C-c ' (=org-edit-special=). This opens another
buffer in =emacs-lisp-mode=, so you can use =M-x eval-buffer= to load
the changes. If you want to explore how functions work, use =M-x
edebug-defun= to set up debugging for that function, and then call it.
You can learn more about edebug in the [[http://www.gnu.org/software/emacs/manual/html_node/elisp/Edebug.html][Emacs Lisp]] manual.

I like using =(setq ...)= more than Customize because I can neatly
organize my configuration that way. Ditto for =use-package= - I mostly
use it to group together package-related config without lots of
=with-eval-after-load= calls, and it also makes declaring keybindings
easier.

=~/.emacs.d/init.el= is tangled directly from this file via
=org-babel-tangle= (=C-c C-v t=). See the auto-tangle hook below.

** Starting up

First line of the tangled =init.el= must declare lexical binding so
closures (lambdas in process sentinels, hooks, etc.) capture their
enclosing locals. Then load =early-init.el= for Emacs <27 (no-op on
modern Emacs since early-init runs on its own).

#+begin_src emacs-lisp
;;; -*- lexical-binding: t; -*-
(require 'early-init (expand-file-name "early-init" user-emacs-directory))
#+end_src

Auto-tangle this file on save so I never have to remember =C-c C-v t=.

#+begin_src emacs-lisp
  (defun jds/tangle-jds-config-on-save ()
    "Tangle `jds-config.org' when it is saved."
    (when (equal (buffer-file-name)
                 (expand-file-name "jds-config.org" user-emacs-directory))
      (let ((org-confirm-babel-evaluate nil)
            (prog-mode-hook nil))
        (org-babel-tangle))))

  (add-hook 'after-save-hook #'jds/tangle-jds-config-on-save)
#+end_src

Tip-of-the-day from the =* Cheatsheet= section above. On every Emacs
startup, =jds/tip= parses the bullets in =* Cheatsheet=, picks one at
random, and pops a small =*emacs-tip*= window. Use =M-x jds/tip= any
time to draw another. Hit =q= to dismiss the window.

This is gamification by way of your own notes: every tip is something
your config already supports, drawn from a section you maintain.

#+begin_src emacs-lisp
  (defvar jds/cheatsheet-file
    (expand-file-name "jds-config.org" user-emacs-directory)
    "Source file for `jds/tip'; expects a top-level `* Cheatsheet' heading.")

  (defun jds/cheatsheet-bullets ()
    "Return bulleted items from the `* Cheatsheet' section as strings.
  Multi-line bullets are joined with spaces; sub-bullets are folded
  into their parent. Returns nil if the section can't be found."
    (when (file-readable-p jds/cheatsheet-file)
      (with-temp-buffer
        (insert-file-contents jds/cheatsheet-file)
        (goto-char (point-min))
        (when (re-search-forward "^\\* Cheatsheet" nil t)
          (let (bullets current)
            (cl-flet ((flush ()
                        (when current
                          (push (mapconcat #'identity
                                           (nreverse current) " ")
                                bullets)
                          (setq current nil))))
              (forward-line 1)
              (catch 'done
                (while (not (eobp))
                  (let ((line (buffer-substring-no-properties
                               (line-beginning-position)
                               (line-end-position))))
                    (cond
                     ((string-match "\\`\\* " line)   ; next top-level
                      (flush) (throw 'done nil))
                     ((string-match "\\`- " line)     ; new bullet
                      (flush)
                      (setq current (list (substring line 2))))
                     ((and current (string-match "\\`[ \t]" line))
                      (push (string-trim line) current))
                     ((string-empty-p (string-trim line))
                      (flush))
                     (t (flush))))
                  (forward-line 1))
                (flush)))
            (nreverse bullets))))))

  (defun jds/tip ()
    "Show a random tip drawn from the `* Cheatsheet' section."
    (interactive)
    (let ((bullets (jds/cheatsheet-bullets)))
      (if (null bullets)
          (message "jds/tip: no cheatsheet bullets found in %s"
                   jds/cheatsheet-file)
        (let ((tip (nth (random (length bullets)) bullets)))
          (with-current-buffer (get-buffer-create "*emacs-tip*")
            (let ((inhibit-read-only t))
              (erase-buffer)
              (when (fboundp 'org-mode) (org-mode))
              (insert "* Try this\n\n")
              (insert "- " tip "\n\n")
              (insert (format "/=M-x jds/tip= for another — %d tips total/\n"
                              (length bullets)))
              (goto-char (point-min))
              (read-only-mode 1))
            (display-buffer (current-buffer)
                            '((display-buffer-pop-up-window)
                              (window-height . 14))))))))

  ;; Show a tip on startup, briefly after Emacs is idle so it doesn't
  ;; race the startup sequence.
  (add-hook 'emacs-startup-hook
            (lambda ()
              (run-with-idle-timer 2 nil #'jds/tip)))
#+end_src

#+BEGIN_SRC emacs-lisp
  (straight-use-package 'use-package)
#+END_SRC


#+BEGIN_SRC emacs-lisp
  (require 'server)
  (unless (server-running-p)
    (server-start))
#+END_SRC

** Personal information

Who I am

#+BEGIN_SRC emacs-lisp
  (setq user-full-name "Jason Sewall"
        user-mail-address "jasewall@nvidia.com")
#+END_SRC

Where I live

#+begin_src emacs-lisp
  (setq calendar-location-name "Pemaquid, ME")
#+end_src

Time zone

#+begin_src emacs-lisp
  (setq calendar-time-zone (* 60 5)
        calendar-standard-time-zone-name "EST"
        calendar-daylight-time-zone-name "EDT")
#+end_src

** Emacs initialization

*** Built-in defaults

One consolidated =(use-package emacs ...)= block for the
plain-old-settings that don't belong to a specific package: backups,
indentation, diffs, encoding, etc. Modes and per-package settings
stay in their own sections below.

#+BEGIN_SRC emacs-lisp
  (use-package emacs
    :straight (:type built-in)
    :init
    (setq use-package-verbose t
          load-prefer-newer t
          inhibit-startup-message t
          ;; Backups
          backup-directory-alist '(("." . "~/.emacs.d/backups"))
          delete-old-versions -1
          version-control t
          vc-make-backup-files t
          auto-save-file-name-transforms '((".*" "~/.emacs.d/auto-save-list/" t))
          ;; Uniquify
          uniquify-buffer-name-style 'reverse
          uniquify-after-kill-buffer-p t
          ;; Editing
          sentence-end-double-space t
          require-final-newline t
          read-quoted-char-radix 16
          ;; Diffs
          diff-switches "-u"
          vc-diff-switches '("-b" "-B" "-u")
          vc-git-diff-switches nil
          vc-follow-symlinks t
          ;; Font lock
          font-lock-maximum-decoration t)
    (setq-default tab-width 2
                  indent-tabs-mode nil)
    :config
    (column-number-mode 1)
    (display-time-mode 1)
    (show-paren-mode 1)
    (global-font-lock-mode 1)
    (tool-bar-mode -1)
    (scroll-bar-mode -1)
    (repeat-mode 1)
    (tab-bar-mode 1)
    (when (fboundp 'pixel-scroll-precision-mode)
      (pixel-scroll-precision-mode 1))
    (fset 'yes-or-no-p 'y-or-n-p))
#+END_SRC

**** macOS modifier mapping

On macOS, remap both Command (⌘) and Option (⌥) to Meta. Don't care
about preserving Super — never use it. We set both =ns-*= (vanilla
GNU Emacs on macOS) and =mac-*= (the Yamamoto =mac= port) so this
works regardless of which build is installed.

Trade-off: Option no longer produces accented characters (⌥-a → å).
If you ever need them, switch to one of:

- =M-x insert-char RET= and pick by Unicode name (vertico-friendly)
- =C-x 8 RET= prefix for common diacritics

#+BEGIN_SRC emacs-lisp
  (when (eq system-type 'darwin)
    (setq ns-command-modifier  'meta
          mac-command-modifier 'meta
          ns-option-modifier   'meta
          mac-option-modifier  'meta))
#+END_SRC

**** Inherit shell PATH (macOS GUI, Linux GUI, daemon mode)

Any time Emacs is launched outside a login shell — Finder/Dock on
macOS, GNOME/KDE/Sway launchers on Linux, =emacs --daemon=, systemd
units — it inherits a minimal PATH that misses =/opt/homebrew/bin=,
=~/.local/bin=, =~/.cargo/bin=, etc. =exec-path-from-shell= runs
your login shell once and copies its environment in. Fixes
=executable-find= for basedpyright, ruff, taplo, pandoc, anything
installed by =uv tool=, =pipx=, =cargo install=, =npm -g=, your
distro's user-bin location.

Window system check covers macOS (=ns=, =mac=), X11 (=x=), Wayland
(=pgtk= and =pure-gtk= when Emacs is built with =--with-pgtk=), and
daemon launches where =window-system= is nil at init time.

#+BEGIN_SRC emacs-lisp
  (use-package exec-path-from-shell
    :straight t
    :if (or (memq window-system '(mac ns x pgtk pure-gtk))
            (daemonp))
    :config
    (dolist (var '("PATH"
                   "MANPATH"
                   "GITHUB_TOKEN"))
      (add-to-list 'exec-path-from-shell-variables var))
    (exec-path-from-shell-initialize))
#+END_SRC

** General configuration
*** Fonts
(set-frame-font "Hack" nil t)
*** Hydra
Keybinding craziness
#+begin_src emacs-lisp
  (use-package hydra
  :straight t)
#+end_src
*** Flymake
Built-in syntax checker. Eglot turns this on automatically in LSP
buffers; we also enable it in prog-mode for non-LSP buffers (elisp,
etc.).

By default flymake only shows the diagnostic message when point is
inside the underlined region. That's annoying when there's a margin
=!= and you've already moved to the line — you shouldn't have to
hunt for the exact column. We override the eldoc integration with a
line-aware variant and add =C-c != to echo any diagnostics on the
current line on demand.

#+BEGIN_SRC emacs-lisp
  (defun jds/flymake-eldoc-on-line (report-doc &rest _)
    "Like `flymake-eldoc-function' but for the whole current line.
  Returns a string suitable for eldoc when there is at least one
  flymake diagnostic anywhere between =line-beginning-position= and
  =line-end-position=."
    (let ((diags (flymake-diagnostics
                  (line-beginning-position) (line-end-position))))
      (when diags
        (funcall report-doc
                 (mapconcat
                  (lambda (d)
                    (format "[%s] %s"
                            (flymake-diagnostic-type d)
                            (flymake-diagnostic-text d)))
                  diags "\n")))))

  (defun jds/flymake-line-message ()
    "Echo flymake diagnostics for the current line, if any."
    (interactive)
    (let ((diags (flymake-diagnostics
                  (line-beginning-position) (line-end-position))))
      (if diags
          (message "%s"
                   (mapconcat
                    (lambda (d)
                      (format "[%s] %s"
                              (flymake-diagnostic-type d)
                              (flymake-diagnostic-text d)))
                    diags "\n"))
        (message "No flymake diagnostics on this line"))))

  (defun jds/setup-flymake-eldoc ()
    "Swap flymake's point-only eldoc reporter for a line-aware one."
    (remove-hook 'eldoc-documentation-functions
                 #'flymake-eldoc-function t)
    (add-hook 'eldoc-documentation-functions
              #'jds/flymake-eldoc-on-line nil t))

  (use-package flymake
    :straight (:type built-in)
    :hook ((prog-mode . flymake-mode)
           (flymake-mode . jds/setup-flymake-eldoc))
    :bind (:map flymake-mode-map
           ("M-n" . flymake-goto-next-error)
           ("M-p" . flymake-goto-prev-error)
           ("C-c ! l" . flymake-show-buffer-diagnostics)
           ("C-c !"   . jds/flymake-line-message)))
#+END_SRC

*** Key-chord
#+BEGIN_SRC emacs-lisp
  (use-package key-chord
    :straight t
    :init (key-chord-mode 1))
#+END_SRC

*** Libraries

#+begin_src emacs-lisp
  (use-package dash
    :straight t)
#+end_src

*** History
Savehist persists minibuffer/kill-ring history across sessions.
#+BEGIN_SRC emacs-lisp
  (setq savehist-file "~/.emacs.d/savehist"
        history-length t
        history-delete-duplicates t
        savehist-save-minibuffer-history 1
        savehist-additional-variables
        '(kill-ring search-ring regexp-search-ring))
  (savehist-mode 1)
#+END_SRC
*** Desktop

Save sesions

#+BEGIN_SRC emacs-lisp
  (desktop-save-mode 1)
  (setq desktop-restore-eager 10)
#+END_SRC

Handy hydra for desktop

#+begin_src emacs-lisp
  (defhydra hydra-desktop (:color blue)
    "desktop"
    ("c" desktop-clear "clear")
    ("s" desktop-save "save")
    ("r" desktop-revert "revert")
    ("d" desktop-change-dir "dir"))
#+end_src

*** Windows configuration

Don't let butterfingers kill it

#+BEGIN_SRC emacs-lisp
  (when window-system
    (global-unset-key "\C-z"))
  (when window-system
    (global-unset-key "\C-x\C-z"))
#+END_SRC

Make it harder to accidentally quit

#+BEGIN_SRC emacs-lisp
  (global-unset-key "\C-x\C-c")
  (global-set-key "\C-x\C-c\C-v" 'save-buffers-kill-emacs)
#+END_SRC

Helpful names

#+BEGIN_SRC emacs-lisp
  (setq frame-title-format
        (concat  "emacs@" (system-name)))
  (unless window-system
    (send-string-to-terminal (concat "\ek" frame-title-format "\e\\")))
#+END_SRC

**** Winner

#+begin_src emacs-lisp
  (use-package ace-window
    :straight t)
  (defhydra hydra-window (global-map "C-x w")
    "
  Movement^^        ^Split^         ^Switch^      ^Resize^
  ----------------------------------------------------------------
  _h_ ←         _v_ertical      _b_uffer        _q_ X←
  _j_ ↓         _x_ horizontal  _f_ind files    _w_ X↓
  _k_ ↑         _z_ undo        _a_ce 1     _e_ X↑
  _l_ →         _Z_ reset       _s_wap      _r_ X→
  _F_ollow        _D_lt Other     _S_ave      max_i_mize
  _SPC_ cancel    _o_nly this     _d_elete
  "
    ("h" windmove-left )
    ("j" windmove-down )
    ("k" windmove-up )
    ("l" windmove-right )
    ("q" hydra-move-splitter-left)
    ("w" hydra-move-splitter-down)
    ("e" hydra-move-splitter-up)
    ("r" hydra-move-splitter-right)
    ("F" follow-mode)
    ("a" (lambda ()
	   (interactive)
	   (ace-window 1)
	   (add-hook 'ace-window-end-once-hook
		     'hydra-window/body))
     )
    ("v" (lambda ()
	   (interactive)
	   (split-window-right)
	   (windmove-right))
     )
    ("x" (lambda ()
	   (interactive)
	   (split-window-below)
	   (windmove-down))
     )
    ("s" (lambda ()
	   (interactive)
	   (ace-window 4)
	   (add-hook 'ace-window-end-once-hook
		     'hydra-window/body)))
    ("S" save-buffer)
    ("d" delete-window)
    ("D" (lambda ()
	   (interactive)
	   (ace-window 16)
	   (add-hook 'ace-window-end-once-hook
		     'hydra-window/body))
     )
    ("b" switch-to-buffer :exit t)
    ("f" find-file :exit t)
    ("o" delete-other-windows)
    ("i" ace-maximize-window)
    ("z" (progn
	   (winner-undo)
	   (setq this-command 'winner-undo))
     )
    ("Z" winner-redo)
    ("SPC" nil))
#+end_src

*** Completion

I got this from here: https://codeberg.org/vifon/emacs-config/src/branch/master/emacs.d/lisp/20-completing-read.el

#+BEGIN_SRC emacs-lisp
;;; -*- lexical-binding: t; -*-

(use-package vertico
  :straight (vertico :files (:defaults "extensions/*"))
  :bind (("C-x M-r" . vertico-repeat)
         :map vertico-map
         ("C-l" . vertico-directory-delete-word)
         ("M-g" . vertico-multiform-grid)
         ("M-q" . vertico-multiform-flat))
  :init (vertico-mode 1)
  :config (progn
            (add-hook 'minibuffer-setup-hook #'vertico-repeat-save)
            (vertico-mouse-mode 1)
            (vertico-multiform-mode 1)
            (setq vertico-multiform-categories '((consult-grep buffer))
                  vertico-multiform-commands '((tmm-menubar flat)
                                               (tmm-shortcut flat)))))

(use-package orderless
  :straight t
  :after vertico
  :config (progn
            (setq orderless-matching-styles '(orderless-regexp
                                              orderless-initialism
                                              orderless-prefixes)
                  orderless-component-separator #'orderless-escapable-split-on-space)

            ;; Use the built-in "partial-completion" style to complete
            ;; file inputs such as "/e/ni/co.nix" into
            ;; "/etc/nixos/configuration.nix".  The "basic" style is
            ;; needed to support the hostname completion in the TRAMP
            ;; inputs such as "/sshx:HOSTNAME".
            (setq completion-category-defaults nil
                  completion-category-overrides '((file (styles basic partial-completion))))

            (setq completion-styles '(orderless))

            (defun vifon/orderless-without-if-bang (pattern index total)
              (when (string-prefix-p "!" pattern)
                `(orderless-without-literal . ,(substring pattern 1))))
            (defun vifon/orderless-literal-if-equal (pattern index total)
              (when (string-suffix-p "=" pattern)
                `(orderless-literal . ,(substring pattern 0 -1))))
            (setq orderless-style-dispatchers '(vifon/orderless-without-if-bang
                                                vifon/orderless-literal-if-equal))))

(use-package embark
  :straight t
  :bind (("C-c o" . embark-act)
         ("C-."   . embark-act)
         :map minibuffer-local-map
         ("M-o"   . embark-act)
         :map embark-command-map
         ;; Unbind the dangerous `global-set-key' and `local-set-key'
         ;; actions.  It's far too easy to accidentally bind over some
         ;; `self-insert-command' binding or even over
         ;; \\[keyboard-quit].
         ("g" . nil)
         ("l" . nil))
  :config (progn
            (setq embark-mixed-indicator-delay 2)

            ;; Make the eval action editable.  Evaluating code
            ;; in-place is simple enough without Embark, if I invoke
            ;; it with Embark, I almost definitely want to edit the
            ;; expression beforehand.  And even if not, I can
            ;; just confirm.
            (cl-pushnew 'embark--allow-edit
                        (alist-get 'pp-eval-expression embark-target-injection-hooks))

            ;; Reload the project list after using
            ;; C-u `embark-act' with `project-forget-project'.
            (cl-pushnew 'embark--restart
                        (alist-get 'project-forget-project embark-post-action-hooks))

            (defun embark-act-with-eval (expression)
              "Evaluate EXPRESSION and call `embark-act' on the result."
              (interactive "sExpression: ")
              (with-temp-buffer
                (let ((expr-value (eval (read expression))))
                  (insert (if (stringp expr-value)
                              expr-value
                            (format "%S" expr-value))))
                (embark-act)))

            (dolist (keymap (list embark-variable-map embark-expression-map))
              (define-key keymap (kbd "v") #'embark-act-with-eval))

            ;; Source: https://github.com/oantolin/embark/wiki/Additional-Actions#attaching-file-to-an-email-message
            (autoload 'gnus-dired-attach "gnus-dired" nil t)
            (defun embark-attach-file (file)
              "Attach FILE to an email message."
              (interactive "fAttach: ")
              (gnus-dired-attach (list file)))
            (bind-key "a" #'embark-attach-file embark-file-map)))

(use-package embark-consult
  :straight t
  :after (embark consult))

(use-package marginalia
  :straight t
  :after vertico
  :demand t                     ; :demand applies to :bind but not
                                ; :after.  We want to eagerly load
                                ; marginalia once vertico is loaded.
  :bind (:map minibuffer-local-map
         ("C-o" . marginalia-cycle))
  :config (marginalia-mode 1))

(use-package consult
  :straight t
  :bind (("M-s f" . consult-line)
         ("M-g g" . consult-line)
         ("M-g o" . consult-outline)
         ("M-g i" . consult-imenu)
         ("M-g r" . consult-ripgrep)
         ("C-x C-r" . consult-recent-file)
         ([remap switch-to-buffer] . consult-buffer)
         ([remap yank-pop] . consult-yank-pop)
         ([remap goto-line] . consult-goto-line)
         :map minibuffer-local-map
         ([remap previous-matching-history-element] . consult-history)
         :map isearch-mode-map
         ("TAB" . vifon/isearch-to-consult-line))
  :config (progn
            (setq consult-project-root-function #'vc-root-dir)
            (consult-customize
             consult-ripgrep consult-grep
             consult-buffer consult-recent-file
             :preview-key "M-.")

            (defun vifon/orderless-fix-consult-tofu (pattern index total)
              "Ignore the last character which is hidden and used only internally."
              (when (string-suffix-p "$" pattern)
                `(orderless-regexp . ,(concat (substring pattern 0 -1)
                                              "[\x200000-\x300000]*$"))))

            (dolist (command '(consult-buffer consult-line))
              (advice-add command :around
                          (lambda (orig &rest args)
                            (let ((orderless-style-dispatchers (cons #'vifon/orderless-fix-consult-tofu
                                                                     orderless-style-dispatchers)))
                              (apply orig args)))))

            ;; Disable consult-buffer project-related capabilities as
            ;; they are very slow in TRAMP.
            (setq consult-buffer-sources
                  (delq 'consult--source-project-buffer
                        (delq 'consult--source-project-file consult-buffer-sources)))

            (setq consult--source-hidden-buffer
                  (plist-put consult--source-hidden-buffer :narrow ?h))

            (defun vifon/isearch-to-consult-line ()
              "Search using `consult-line' what was being searched with `isearch'."
              (interactive)
              (isearch-exit)
              (let ((query (if isearch-regexp
                               isearch-string
                             (regexp-quote isearch-string))))
                (consult-line query)))))

(use-package corfu
  :straight (corfu :files (:defaults "extensions/*"))
  :init (global-corfu-mode 1)
  :custom
  (corfu-auto t)
  (corfu-auto-delay 0.2)
  (corfu-cycle t)
  :config
  (require 'corfu-popupinfo)
  (corfu-popupinfo-mode 1))

;; Completion-at-point extensions. Plays the role company-backends did
;; for company-mode: dabbrev, file paths, keywords, abbrevs, etc.
(use-package cape
  :straight t
  :init
  (add-to-list 'completion-at-point-functions #'cape-dabbrev)
  (add-to-list 'completion-at-point-functions #'cape-file)
  (add-to-list 'completion-at-point-functions #'cape-keyword))


;;; https://with-emacs.com/posts/tutorials/customize-completion-at-point/
(autoload 'ffap-file-at-point "ffap")
(add-hook 'completion-at-point-functions
          (defun complete-path-at-point+ ()
            (let ((fn (ffap-file-at-point))
                  (fap (thing-at-point 'filename)))
              (when (and (or fn (equal "/" fap))
                         (save-excursion
                           (search-backward fap (line-beginning-position) t)))
                (list (match-beginning 0)
                      (match-end 0)
                      #'completion-file-name-table :exclusive 'no))))
          'append)

;;; Add prompt indicator to `completing-read-multiple'.
;;; We display [CRM<separator>], e.g., [CRM,] if the separator is a comma.
;;;
;;; Taken from the Vertico docs.
(defun crm-indicator (args)
  (cons (format "[CRM%s] %s"
                (replace-regexp-in-string
                 "\\`\\[.*?]\\*\\|\\[.*?]\\*\\'" ""
                 crm-separator)
                (car args))
        (cdr args)))
(advice-add #'completing-read-multiple :filter-args #'crm-indicator)

(setq enable-recursive-minibuffers t)
(minibuffer-depth-indicate-mode 1)

;;; Use the completing-read UI for the M-tab completion unless
;;; overridden (for example by `corfu').
(setq-default completion-in-region-function
              (lambda (&rest args)
                (apply (if vertico-mode
                           #'consult-completion-in-region
                         #'completion--in-region)
                       args)))
#+END_SRC

*** Appearances

Prot's ef-themes — pleasant, well-balanced, many variants. Try
=M-x ef-themes-select= to browse, or =M-x ef-themes-toggle= to
switch between a preferred light/dark pair.

#+BEGIN_SRC emacs-lisp
  (use-package ef-themes
    :straight t
    :config
    (setq ef-themes-to-toggle '(ef-dream ef-frost))
    (load-theme 'ef-dream :no-confirm))
#+END_SRC

=minions= collapses the long list of minor-mode lighters in the
modeline into a single icon you can click to expand.

#+BEGIN_SRC emacs-lisp
  (use-package minions
    :straight t
    :config (minions-mode 1))
#+END_SRC

=breadcrumb= shows your location (imenu / project) in the header
line. Helpful when navigating big files.

#+BEGIN_SRC emacs-lisp
  (use-package breadcrumb
    :straight t
    :hook ((prog-mode . breadcrumb-local-mode)
           (text-mode . breadcrumb-local-mode)))
#+END_SRC

*** Unicode

#+BEGIN_SRC emacs-lisp
  (prefer-coding-system 'utf-8)
  (setq-default buffer-file-coding-system 'utf-8-unix)
  (set-default-coding-systems 'utf-8-unix)
  (setq-default default-buffer-file-coding-system 'utf-8-unix)
  (define-coding-system-alias 'UTF-8 'utf-8)

  (defmacro my/insert-unicode (unicode-name)
    `(lambda () (interactive)
       (insert-char (cdr (assoc-string ,unicode-name (ucs-names))))))

  (when (display-graphic-p)
    (setq x-select-request-type '(UTF8_STRING COMPOUND_TEXT TEXT STRING)))
#+END_SRC

*** Expand

#+BEGIN_SRC emacs-lisp
  (bind-key "M-/" 'hippie-expand)
#+END_SRC

From https://github.com/purcell/emacs.d/blob/master/lisp/init-auto-complete.el - Exclude very large buffers from dabbrev
#+BEGIN_SRC emacs-lisp
  (defun sanityinc/dabbrev-friend-buffer (other-buffer)
    (< (buffer-size other-buffer) (* 1 1024 1024)))
  (setq dabbrev-friend-buffer-function 'sanityinc/dabbrev-friend-buffer)
#+END_SRC

#+BEGIN_SRC emacs-lisp
  (setq hippie-expand-try-functions-list
        '(try-expand-all-abbrevs
          try-complete-file-name-partially
          try-complete-file-name
          try-expand-dabbrev
          try-expand-dabbrev-from-kill
          try-expand-dabbrev-all-buffers
          try-expand-list
          try-expand-line
          try-complete-lisp-symbol-partially
          try-complete-lisp-symbol))
#+END_SRC

*** Diminish

#+BEGIN_SRC emacs-lisp
  (use-package diminish
    :straight t
    :init (diminish 'eldoc-mode))
#+END_SRC

*** Eglot

#+BEGIN_SRC emacs-lisp
  (use-package eglot
    :straight (:type built-in)
    :config
    (add-to-list 'eglot-server-programs
                 '(c-mode c++-mode cuda-mode
                          . ("clangd"
                             "-j=4"
                             "--malloc-trim"
                             "--log=error"
                             "--background-index"
                             "--clang-tidy"
                             "--cross-file-rename"
                             "--completion-style=detailed"
                             "--pch-storage=memory"
                             "--header-insertion=never"
                             "--header-insertion-decorators=0")))
    ;; basedpyright for type-aware completion, hover docs, and rename.
    ;; Ruff is handled separately by apheleia (format) and could also
    ;; be run as a flymake lint via flymake-ruff if you wanted, but
    ;; basedpyright's diagnostics + mypy + ty already cover lots.
    (add-to-list 'eglot-server-programs
                 '(python-ts-mode . ("basedpyright-langserver" "--stdio"))))

#+END_SRC

*** Tree-sitter

=treesit= is built into Emacs 29+. =treesit-auto= auto-installs
grammars and remaps modes to their =-ts-mode= variants (so
=python-mode= → =python-ts-mode=, =rust-mode= → =rust-ts-mode=, etc.).

#+BEGIN_SRC emacs-lisp
  (use-package treesit-auto
    :straight t
    :custom
    (treesit-auto-install 'prompt)
    :config
    (treesit-auto-add-to-auto-mode-alist 'all)
    (global-treesit-auto-mode))
#+END_SRC

*** eldoc-box — floating signature/doc popups

The default eldoc echo area is cramped for LSP-style signatures.
=eldoc-box= pops a child frame instead. Toggle with =C-h .= or hover.

#+BEGIN_SRC emacs-lisp
  (use-package eldoc-box
    :straight t
    :hook (eglot-managed-mode . eldoc-box-hover-mode)
    :custom
    (eldoc-box-max-pixel-width 600)
    (eldoc-box-max-pixel-height 400))
#+END_SRC

** System stuff
*** Tramp
Real handy when working on remote machines
#+begin_src emacs-lisp
  (use-package tramp
    :straight (:type built-in)
    :init (setq tramp-unified-filename t))
#+end_src
*** Shells
**** Remote term access

This lets me open up terminals. I wish I could get this to work with tramp.

#+begin_src emacs-lisp
  (add-hook 'term-mode-hook
      (lambda ()
        (setq term-buffer-maximum-size 100000)))

  ;; Use this for remote so I can specify command line arguments
  (defun my/remote-term (new-buffer-name cmd &rest switches)
    (setq term-ansi-buffer-name (concat "*" new-buffer-name "*"))
    (setq term-ansi-buffer-name (generate-new-buffer-name term-ansi-buffer-name))
    (setq term-ansi-buffer-name (apply 'make-term term-ansi-buffer-name cmd nil switches))
    (set-buffer term-ansi-buffer-name)
    (term-mode)
    (term-char-mode)
    (let (term-escape-char)
      ;; I wanna have find-file on C-x C-f -mm
      ;; your mileage may definitely vary, maybe it's better to put this in your
      ;; .emacs ...
      (term-set-escape-char ?\C-x))

    (switch-to-buffer term-ansi-buffer-name))

  (use-package pcomplete
    :straight (:type built-in)
    :init (progn (require 'pcmpl-unix) (defun my/ssh-remote-term (hostname)
                                         (interactive (list (completing-read "Hostname: " (pcmpl-ssh-hosts))))
                                         (my/remote-term hostname "ssh" hostname))))

  ;; (defun helm-source-ssh-remote-term ()
  ;;   (helm-build-sync-source "SSH hostname"
  ;;     :candidates (lambda () (pcmpl-ssh-hosts))
  ;;     :filtered-candidate-transformer '(helm-adaptive-sort)
  ;;     :nomark t
  ;;     :action '(("Select host" . my/ssh-remote-term))))

  ;; (defun my/helm-ssh-remote-term ()
  ;;   (interactive)
  ;;   (helm :sources (helm-source-ssh-remote-term)
  ;;         :buffer "*helm-ssh-remote-term*"))

  (defun my/local-term ()
    (interactive)
    (ansi-term "bash" "localhost"))
#+end_src

**** Eat
#+begin_src emacs-lisp
(straight-use-package
 '(eat :type git
       :host codeberg
       :repo "akib/emacs-eat"
       :files ("*.el" ("term" "term/*.el") "*.texi"
               "*.ti" ("terminfo/e" "terminfo/e/*")
               ("terminfo/65" "terminfo/65/*")
               ("integration" "integration/*")
               (:exclude ".dir-locals.el" "*-tests.el"))))
#+end_src
**** Customize shells
#+begin_src emacs-lisp
  (add-hook 'shell-mode-hook 'ansi-color-for-comint-mode-on)
  (add-hook 'comint-output-filter-functions 'comint-watch-for-password-prompt)
#+end_src
*** Ibuffer
#+begin_src emacs-lisp
  (use-package ibuffer
    :straight (:type built-in)
    :bind (("<f9>" . ibuffer))
    :init (setq ibuffer-shrink-to-minimum-size t
                ibuffer-always-show-last-buffer nil
                ibuffer-sorting-mode 'recency
                ibuffer-use-header-line t))
#+end_src
** Projects & version control
*** Git
I don't know if this is actually necessary
#+begin_src emacs-lisp
  (add-to-list 'vc-handled-backends 'GIT)
#+end_src
*** Git-link
#+begin_src emacs-lisp
  (use-package git-link
    :straight t
    :init (global-set-key (kbd "C-c m l") 'git-link))
#+end_src
*** YADM
#+begin_src emacs-lisp
  (add-to-list 'tramp-methods
               '("yadm"
                 (tramp-login-program "yadm")
                 (tramp-login-args (("enter")))
                 (tramp-remote-shell "/bin/sh")
                 (tramp-remote-shell-args ("-c"))))
#+end_src
*** Magit
#+begin_src emacs-lisp
  (use-package magit
    :straight t
    :init (setq magit-auto-revert-mode t)
    :bind (("C-x C-g" . magit-status)))
#+end_src
*** Projects
Use the built-in =project.el=. Standard prefix is =C-x p= (e.g. =C-x p
f= find file in project, =C-x p g= grep, =C-x p p= switch project).
=consult-buffer= and =magit= integrate with this natively.
#+begin_src emacs-lisp
  (use-package project
    :straight (:type built-in))
#+end_src
** Navigation & Search
*** Go to line
#+begin_src emacs-lisp
  (global-set-key "\C-cg" 'goto-line)

  (use-package avy
    :straight t
    :init (defhydra hydra-avy (global-map "M-g" :color blue)
	    "avy-goto"
	    ("c" avy-goto-char "char")
	    ("C" avy-goto-char-2 "char-2")
	    ("w" avy-goto-word-1 "word")
	    ("s" avy-goto-subword-1 "subword")
	    ("u" link-hint-open-link "open-URI")
	    ("U" link-hint-copy-link "copy-URI"))
    :bind (("M-g g" . avy-goto-line)))

  (defhydra hydra-goto-line (goto-map ""
				      :pre (display-line-numbers-mode 1)
				      :post (display-line-numbers-mode -1))
    "goto-line"
    ("g" goto-line "go")
    ("m" set-mark-command "mark" :bind nil)
    ("q" nil "quit"))
#+end_src
*** Menu bar via keyboard
Navigate menus via keyboard using consult.
#+begin_src emacs-lisp
  (global-set-key (kbd "<f10>") #'consult-menu-bar)
#+end_src

*** Window movement
Use arrow keys to switch windows.
#+begin_src emacs-lisp
  (use-package windmove
    :straight (:type built-in)
    :init (windmove-default-keybindings))
  (global-set-key "\M-o" 'other-window)
#+end_src
*** Move to start
#+begin_src emacs-lisp
  (defun my/smarter-move-beginning-of-line (arg)
    "Move point back to indentation of beginning of line.

  Move point to the first non-whitespace character on this line.
  If point is already there, move to the beginning of the line.
  Effectively toggle between the first non-whitespace character and
  the beginning of the line.

  If ARG is not nil or 1, move forward ARG - 1 lines first.  If
  point reaches the beginning or end of the buffer, stop there."
    (interactive "^p")
    (setq arg (or arg 1))

    ;; Move lines first
    (when (/= arg 1)
      (let ((line-move-visual nil))
        (forward-line (1- arg))))

    (let ((orig-point (point)))
      (back-to-indentation)
      (when (= orig-point (point))
        (move-beginning-of-line 1))))

  ;; remap C-a to `smarter-move-beginning-of-line'
  (global-set-key [remap move-beginning-of-line]
                  'my/smarter-move-beginning-of-line)
#+end_src
*** Ripgrep
#+begin_src emacs-lisp
  (use-package rg
    :straight t
    :init (rg-enable-default-bindings))
#+end_src
*** Phi-search
#+begin_src emacs-lisp
  (use-package phi-search
    :straight t
    :diminish phi-search
    :after multiple-cursors
    :bind (:map mc/keymap
           ("C-s" . phi-search)
           ("C-r" . phi-search-backward)))
#+end_src
** Editing tricks
*** Yasnippet
#+begin_src emacs-lisp
  (use-package yasnippet
    :straight t
    :init (yas-global-mode 1))
#+end_src

*** Which Key
#+begin_src emacs-lisp
  (use-package which-key
    :straight t
    :init (which-key-mode))
#+end_src

*** Aggresive indentation
#+begin_src emacs-lisp
  (use-package aggressive-indent
    :straight t)
#+end_src

*** Multiple cursors
So powerful
#+begin_src emacs-lisp
  (use-package multiple-cursors
    :straight t
    :init (defhydra multiple-cursors-hydra (global-map "C-x m")
	    "
       ^Up^            ^Down^        ^Other^
  ----------------------------------------------
  [_p_]   Next    [_n_]   Next    [_l_] Edit lines
  [_P_]   Skip    [_N_]   Skip    [_a_] Mark all
  [_M-p_] Unmark  [_M-n_] Unmark  [_r_] Mark by regep
  ^ ^             ^ ^             [_i_] Insert numbers
  ^ ^             ^ ^             [_h_] Hide unmatched
  ^ ^             ^ ^             [_s_] Sort regions
  ^ ^             ^ ^             [_q_] Quit
  "
	    ("i" mc/insert-numbers)
	    ("h" mc-hide-unmatched-lines-mode)
	    ("s" mc/sort-regions)
	    ("l" mc/edit-lines :exit t)
	    ("a" mc/mark-all-like-this :exit t)
	    ("n" mc/mark-next-like-this)
	    ("N" mc/skip-to-next-like-this)
	    ("M-n" mc/unmark-next-like-this)
	    ("p" mc/mark-previous-like-this)
	    ("P" mc/skip-to-previous-like-this)
	    ("M-p" mc/unmark-previous-like-this)
	    ("r" mc/mark-all-in-region-regexp :exit t)
	    ("q" nil))
    :bind (("C-^" . set-rectangular-region-anchor)
	   ("M-3" . mc/mark-next-like-this)
	   ("M-4" . mc/mark-previous-like-this)
	   ("M-#" . mc/unmark-next-like-this)
	   ("M-$" . mc/unmark-previous-like-this)))
#+end_src

*** Expand region
#+begin_src emacs-lisp
  (use-package expand-region
    :straight t
    :defer t
    :bind (("M-2" . er/expand-region)))
#+end_src
*** Transpose, but keep whitespace
#+begin_src emacs-lisp
  (defun my/forward-transpose-whitespace (begin end)
    "If mark is active, swap leading whitespace with region between
        point and mark. If mark isn't active, find the first
        non-whitespace character after point and swap it with the
        whitespace before it. To start, place point on character or at
        start of region."
    (interactive "*r")
    (let* ((string-to-be-switched
            (if (use-region-p)
                (delete-and-extract-region begin end)
              (progn
                (skip-chars-forward "[:space:]")
                (delete-and-extract-region (point) (1+ (point))))))
           (right-anchor (point))
           (whitespace
            (progn
              (skip-chars-backward "[:space:]")
              (delete-and-extract-region (point) right-anchor))))
      (insert string-to-be-switched whitespace)))

  (global-set-key (kbd "C-c t") 'my/forward-transpose-whitespace)
#+end_src
*** Unfill paragraph

#+BEGIN_SRC emacs-lisp
  (defun my/unfill-paragraph (&optional region)
    "Takes a multi-line paragraph and makes it into a single line of text."
    (interactive (progn
                   (barf-if-buffer-read-only)
                   (list t)))
    (let ((fill-column (point-max)))
      (fill-paragraph nil region)))
  (bind-key "C-x M-q" 'my/unfill-paragraph)
#+END_SRC

I never actually justify text, so I might as well change the way
=fill-paragraph= works. With the code below, =M-q= will fill the
paragraph normally, and =C-u M-q= will unfill it.

#+BEGIN_SRC emacs-lisp
  (defun my/fill-or-unfill-paragraph (&optional unfill region)
    "Fill paragraph (or REGION).
    With the prefix argument UNFILL, unfill it instead."
    (interactive (progn
                   (barf-if-buffer-read-only)
                   (list (if current-prefix-arg 'unfill) t)))
    (let ((fill-column (if unfill (point-max) fill-column)))
      (fill-paragraph nil region)))
  (bind-key "M-q" 'my/fill-or-unfill-paragraph)
#+END_SRC

Also, =visual-line-mode= is so much better than =auto-fill-mode=. It doesn't actually break the text into multiple lines - it only looks that way.

#+BEGIN_SRC emacs-lisp
  (remove-hook 'text-mode-hook #'turn-on-auto-fill)
  (add-hook 'text-mode-hook 'turn-on-visual-line-mode)
#+END_SRC

*** Whitespace

#+BEGIN_SRC emacs-lisp
  (bind-key "M-SPC" 'cycle-spacing)
#+END_SRC

=ws-butler= trims trailing whitespace, but only on lines you've
actually edited — so diffs stay focused. Don't combine with a global
=delete-trailing-whitespace= on =before-save-hook=; that defeats the
surgical behavior.

#+BEGIN_SRC emacs-lisp
  (use-package ws-butler
    :straight t
    :hook (prog-mode . ws-butler-mode))
#+END_SRC

*** Vundo — visualize the undo tree

=vundo= visualizes Emacs's built-in undo system without replacing it
(unlike =undo-tree=). Use =C-x u= to pop the visualizer; navigate with
=p=/=n=/=f=/=b= and exit with =q=.

#+BEGIN_SRC emacs-lisp
  (use-package vundo
    :straight t
    :bind (("C-x u" . vundo))
    :custom
    (vundo-glyph-alist vundo-unicode-symbols))
#+END_SRC

** Editing modes
*** Semantic support
*** Literate programming
**** Editing source code
I don't want to get distracted by the same code in the other window, so I want org src to use the current window.

#+begin_src emacs-lisp
  (setq org-src-window-setup 'current-window)
#+end_src

**** org-modern + org-appear

=org-modern= modernizes headlines, blocks, lists, checkboxes.
=org-appear= reveals the underlying markup (=*bold*=, =/italic/=,
links) when point lands on it, so you can edit in place without
toggling =org-toggle-link-display=.

#+begin_src emacs-lisp
  (use-package org-modern
    :straight t
    :hook (org-mode . org-modern-mode))

  (use-package org-appear
    :straight t
    :hook (org-mode . org-appear-mode))
#+end_src

**** Copying and sharing code

#+begin_src emacs-lisp
  (defun my/copy-code-as-org-block-and-gist (beg end)
    (interactive "r")
    (let ((filename (file-name-base))
          (mode (symbol-name major-mode))
          (contents
           (if (use-region-p) (buffer-substring beg end) (buffer-string)))
          (gist (if (use-region-p) (gist-region beg end) (gist-buffer))))
      (kill-new
       (format "\n[[%s][Gist: %s]]\n#+begin_src %s\n%s\n#+end_src\n"
               (oref (oref gist :data) :html-url) filename
               (replace-regexp-in-string "-mode$" "" mode)
               contents))))
#+end_src

*** Compilation

Use C-c C-m to compile

#+begin_src emacs-lisp
  (use-package compile
    :straight (:type built-in)
    :init (progn
	    (add-hook 'c-mode-common-hook (lambda () (local-set-key "\C-c\C-m" 'compile)))
	    (add-hook 'fortran-mode-hook (lambda () (local-set-key "\C-c\C-m" 'compile)))
	    (add-hook 'f90-mode-hook (lambda () (local-set-key "\C-c\C-m" 'compile)))
	    (add-hook 'makefile-gmake-mode-hook (lambda () (local-set-key "\C-c\C-m" 'compile)))
	    (add-hook 'compilation-mode-hook (lambda () (local-set-key "\C-c\C-m" 'compile))))
    (setq compilation-scroll-output 'first-error))
#+end_src

Add a hydra for navigating compilation logs

#+begin_src emacs-lisp
  (defhydra hydra-next-error
    (global-map "C-x")
    "
  Compilation errors:
  _j_: next error        _h_: first error    _q_uit
  _k_: previous error    _l_: last error
  "
    ("`" next-error     nil)
    ("j" next-error     nil :bind nil)
    ("k" previous-error nil :bind nil)
    ("h" first-error    nil :bind nil)
    ("l" (condition-case err
             (while t
               (next-error))
           (user-error nil))
     nil :bind nil)
    ("q" nil            nil :color blue))
#+end_src

*** LaTeX
#+begin_src emacs-lisp
  (use-package auctex
    :straight t
    :defer t
    :config (progn (setq TeX-PDF-mode t)
		   (add-hook 'LaTeX-mode-hook '(lambda () (flyspell-mode 1)))))
#+end_src
*** Common Lisp
=sly= is a maintained fork of =slime= with a nicer UX.
#+begin_src emacs-lisp
  (use-package sly
    :straight t
    :custom
    (inferior-lisp-program "sbcl"))
#+end_src
*** Elisp
#+begin_src emacs-lisp
  (defun my/eval-and-replace ()
    "Replace the preceding sexp with its value."
    (interactive)
    (backward-kill-sexp)
    (condition-case nil
        (prin1 (eval (read (current-kill 0)))
               (current-buffer))
      (error (message "Invalid expression")
             (insert (current-kill 0)))))

  (global-set-key (kbd "C-x C-e") 'my/eval-and-replace)
#+end_src
*** Python

2026 uv-first stack:

- =python-ts-mode= for tree-sitter highlighting/imenu
- =pet= auto-detects =.venv= / =pyproject.toml= / =.python-version=
  per project (works with uv out of the box) and tells eglot, mypy,
  ty, ruff, etc. where to find executables and interpreter
- =pyvenv= provides the activation primitive =pet= drives
- =basedpyright= as the type-aware LSP via eglot
- =ruff= for format-on-save via =apheleia=
- =flymake-mypy= shows mypy diagnostics alongside basedpyright
- a small custom flymake backend runs =ty check= as a second layer

**** python-ts-mode + pet (uv-aware env detection)

#+begin_src emacs-lisp
  (use-package python
    :straight (:type built-in)
    :hook (python-ts-mode . eglot-ensure)
    :mode (("\\.py\\'" . python-ts-mode)))

  ;; pyvenv supplies the activation primitives that pet drives.
  (use-package pyvenv
    :straight t
    :after python
    :config
    (add-to-list 'global-mode-string
                 '(pyvenv-virtual-env-name (" venv:" pyvenv-virtual-env-name " "))
                 'append))

  ;; pet (Python Environment Tool) auto-detects the right venv per
  ;; project — looks at .venv/, .python-version, pyproject.toml. Works
  ;; with uv, poetry, pipenv, rye out of the box, no per-project setup.
  ;; pet-mode wires its findings into pyvenv, eglot, flymake-mypy, etc.
  (use-package pet
    :straight t
    :config
    (add-hook 'python-base-mode-hook #'pet-mode -10))
#+end_src

**** Sanity check: what's missing?

The first time a Python buffer opens (and on demand via
=M-x jds/python-check-tools=), we look for the CLI tools the rest of
this section depends on and pop up a small report listing anything
missing with the exact =uv tool install ...= line to run. Uses
=pet-executable-find= when pet is loaded so it respects the project
venv; falls back to plain =executable-find= otherwise.

#+begin_src emacs-lisp
  (defvar jds/python-required-tools
    '(("basedpyright-langserver" "basedpyright" "type-aware LSP (eglot)")
      ("ruff"                    "ruff"         "formatter & linter (apheleia)")
      ("mypy"                    "mypy"         "static type checker (flymake-mypy)")
      ("ty"                      "ty"           "Astral type checker (flymake)"))
    "List of (EXECUTABLE UV-PACKAGE DESCRIPTION) for Python tooling.")

  (defvar jds/python-tools--checked nil
    "Non-nil once we've reported on missing Python tools this session.")

  (defun jds/python--find-tool (exe)
    "Locate EXE, preferring the project venv via pet when available."
    (or (and (fboundp 'pet-executable-find) (pet-executable-find exe))
        (executable-find exe)))

  (defun jds/python-check-tools (&optional force)
    "Report any of `jds/python-required-tools' that aren't on PATH.
  With prefix arg, re-check even if already reported this session."
    (interactive "P")
    (when (or force (not jds/python-tools--checked))
      (setq jds/python-tools--checked t)
      (let (missing)
        (dolist (entry jds/python-required-tools)
          (unless (jds/python--find-tool (car entry))
            (push entry missing)))
        (cond
         (missing
          (with-current-buffer (get-buffer-create "*python-tools*")
            (let ((inhibit-read-only t))
              (erase-buffer)
              (insert "Python tools not found on PATH (or in project venv):\n\n")
              (dolist (m (nreverse missing))
                (insert (format "  • %s\n      %s\n      install: uv tool install %s\n\n"
                                (car m) (nth 2 m) (nth 1 m))))
              (insert "After installing, run M-x jds/python-check-tools to re-verify.\n")
              (insert "Or for project-local tools: uv add --dev <pkg> then uv sync.\n")
              (special-mode))
            (display-buffer (current-buffer))))
         (force
          (message "Python: all expected tools found"))))))

  (add-hook 'python-base-mode-hook #'jds/python-check-tools)
#+end_src

**** apheleia for ruff format-on-save

#+begin_src emacs-lisp
  (use-package apheleia
    :straight (apheleia
               :type git
               :host github
               :repo "radian-software/apheleia")
    :hook (python-ts-mode . apheleia-mode)
    :config
    ;; Use ruff format (apheleia knows ruff already; this makes sure
    ;; python-ts-mode buffers route to it).
    (setf (alist-get 'python-ts-mode apheleia-mode-alist) 'ruff))
#+end_src

**** flymake-mypy and a small ty backend

=flymake-mypy= runs mypy as a flymake checker. =pet= makes sure the
mypy invoked is the one inside the project's venv.

#+begin_src emacs-lisp
  (use-package flymake-mypy
    :straight (flymake-mypy :type git :host github :repo "com4/flymake-mypy")
    :hook (python-base-mode . flymake-mypy-enable))
#+end_src

ty (Astral's type checker) doesn't have a stable LSP yet, so we wire
=ty check= as a small flymake backend. Adjust the regex if upstream
output format changes.

#+begin_src emacs-lisp
  (defvar-local jds/flymake-ty--proc nil)

  (defun jds/flymake-ty-backend (report-fn &rest _args)
    "Flymake backend running =ty check= on the current buffer's file."
    (let ((file (buffer-file-name))
          (source (current-buffer)))
      (when (and file (executable-find "ty"))
        (when (process-live-p jds/flymake-ty--proc)
          (kill-process jds/flymake-ty--proc))
        (setq
         jds/flymake-ty--proc
         (make-process
          :name "flymake-ty" :noquery t :connection-type 'pipe
          :buffer (generate-new-buffer " *flymake-ty*")
          :command (list "ty" "check" "--output-format" "concise" file)
          :sentinel
          (lambda (proc _event)
            (when (memq (process-status proc) '(exit signal))
              (unwind-protect
                  (when (buffer-live-p source)
                    (with-current-buffer (process-buffer proc)
                      (goto-char (point-min))
                      (let (diags)
                        ;; concise format: "file:line:col: severity[code]: message"
                        (while (re-search-forward
                                "^.+:\\([0-9]+\\):\\([0-9]+\\):? *\\(error\\|warning\\|info\\)[^:]*: *\\(.+\\)$"
                                nil t)
                          (let* ((line (string-to-number (match-string 1)))
                                 (col  (string-to-number (match-string 2)))
                                 (sev  (match-string 3))
                                 (msg  (match-string 4))
                                 (type (pcase sev
                                         ("error"   :error)
                                         ("warning" :warning)
                                         (_         :note)))
                                 (region (with-current-buffer source
                                           (flymake-diag-region source line col))))
                            (when region
                              (push (flymake-make-diagnostic
                                     source (car region) (cdr region)
                                     type (concat "[ty] " msg))
                                    diags))))
                        (funcall report-fn diags))))
                (kill-buffer (process-buffer proc))))))))))

  (defun jds/enable-ty-flymake ()
    (add-hook 'flymake-diagnostic-functions #'jds/flymake-ty-backend nil t))

  (add-hook 'python-base-mode-hook #'jds/enable-ty-flymake)
#+end_src
*** Rust
#+begin_src emacs-lisp
  (use-package rustic
    :straight t
    :bind (:map rustic-mode-map
                ("M-j" . imenu)
                ("M-?" . xref-find-references)
                ("C-c C-c l" . flymake-show-buffer-diagnostics)
                ("C-c C-c a" . eglot-code-actions)
                ("C-c C-c r" . eglot-rename)
                ("C-c C-c q" . eglot-reconnect)
                ("C-c C-c Q" . eglot-shutdown))
    :custom
    (rustic-lsp-client 'eglot)
    (rustic-lsp-server 'rust-analyzer)
    (rustic-analyzer-command '("rustup run stable rust-analyzer"))
    :config
    (setq rustic-format-on-save t))
#+end_src
*** Fortran
#+begin_src emacs-lisp
  (setq fortran-comment-region "!"
        fortran-line-length 200)
#+end_src
*** Markdown

=markdown-mode= is mature and battle-tested (still preferred over the
treesit variant). =markdown-fontify-code-blocks-natively= turns on
syntax highlighting inside fenced code blocks using the appropriate
major mode. =marksman= is a fast LSP for cross-document refs / wiki
links — useful in docs-heavy projects. Install:

- macOS: =brew install marksman=
- Linux: grab the static binary from
  https://github.com/artempyanykh/marksman/releases and drop it on
  =$PATH= (no distro packages I'm aware of).

#+begin_src emacs-lisp
  (use-package markdown-mode
    :straight t
    :mode (("\\.md\\'"       . markdown-mode)
           ("\\.markdown\\'" . markdown-mode))
    :hook ((markdown-mode . pandoc-mode)
           (markdown-mode . visual-line-mode))
    :custom
    (markdown-fontify-code-blocks-natively t)
    (markdown-header-scaling t)
    (markdown-hide-urls nil))

  (use-package pandoc-mode
    :straight t)

  (with-eval-after-load 'eglot
    (add-to-list 'eglot-server-programs
                 '(markdown-mode . ("marksman"))))
#+end_src

**** Reading mode for prose

A single toggle that combines:

- =markdown-toggle-markup-hiding= — hide =*=, =_=, =[]()= so headings,
  bold, italics, and links show clean
- =mixed-pitch-mode= — proportional font for prose, monospace stays
  on code blocks and inline =verbatim=
- =olivetti-mode= — centered narrow column (~80ch) for comfortable
  line length

=M-x jds/markdown-reading-mode= flips between the two states. Pairs
with the existing =visual-line-mode= (soft wrap) for a clean reading
view without leaving Emacs.

#+begin_src emacs-lisp
  (use-package olivetti
    :straight t
    :custom (olivetti-body-width 80))

  (use-package mixed-pitch
    :straight t
    :config
    ;; Keep tables, code blocks, inline code, and anything else
    ;; alignment-sensitive in monospace, even with mixed-pitch on.
    (dolist (face '(markdown-table-face
                    markdown-code-face
                    markdown-inline-code-face
                    markdown-pre-face
                    markdown-language-keyword-face
                    markdown-language-info-face
                    markdown-url-face))
      (add-to-list 'mixed-pitch-fixed-pitch-faces face)))

  (define-minor-mode jds/markdown-reading-mode
    "Toggle prose-friendly rendering for a markdown buffer."
    :lighter " 📖"
    (if jds/markdown-reading-mode
        (progn
          (markdown-toggle-markup-hiding 1)
          (mixed-pitch-mode 1)
          (olivetti-mode 1))
      (markdown-toggle-markup-hiding -1)
      (mixed-pitch-mode -1)
      (olivetti-mode -1)))
#+end_src

**** Live preview via pandoc (built into markdown-mode)

=markdown-mode= has =markdown-live-preview-mode= built in: it renders
the buffer with whatever shell command =markdown-command= names and
shows the result in a side-window EWW buffer. The preview
auto-refreshes as you type. Fully local, no server, no tokens, no
external service.

We point =markdown-command= at =pandoc= with GitHub-flavored markdown
input and HTML5 output. Install pandoc:

- macOS: =brew install pandoc=
- Debian/Ubuntu: =sudo apt install pandoc=
- Fedora/RHEL: =sudo dnf install pandoc=
- Arch: =sudo pacman -S pandoc=

- =C-c C-c l= — toggle =markdown-live-preview-mode= (live EWW buffer)
- =C-c C-c p= — one-shot =markdown-preview= (opens once in default
  browser; doesn't refresh)
- =C-c C-c v= — =markdown-export-and-preview= (export to file, open
  in browser)

#+begin_src emacs-lisp
  (with-eval-after-load 'markdown-mode
    (setq markdown-command "pandoc -f gfm -t html5 --standalone"))
#+end_src

*** TOML

Tree-sitter-backed mode comes from =toml-ts-mode= (built-in; grammar
auto-installed by =treesit-auto=). =taplo= is the LSP — formatter +
schema-aware validation (it knows =pyproject.toml=,
=Cargo.toml=, etc.). Install:

- macOS: =brew install taplo=
- Cross-platform: =cargo install taplo-cli --features lsp= (needs
  Rust toolchain), or download a release binary from
  https://github.com/tamasfe/taplo/releases

#+begin_src emacs-lisp
  (with-eval-after-load 'eglot
    (add-to-list 'eglot-server-programs
                 '(toml-ts-mode . ("taplo" "lsp" "stdio"))))

  (add-hook 'toml-ts-mode-hook #'eglot-ensure)
#+end_src

*** YAML

=yaml-ts-mode= (built-in; grammar via =treesit-auto=). The LSP is
=yaml-language-server= (Red Hat), which understands K8s manifests,
GitHub Actions, GitLab CI, JSON Schema, and a bunch of others.
Install:

- macOS: =brew install yaml-language-server=
- Cross-platform: =npm install -g yaml-language-server= (needs
  Node), or grab a release from
  https://github.com/redhat-developer/yaml-language-server/releases

YAML is whitespace-sensitive so we also pin indentation explicitly.

#+begin_src emacs-lisp
  (with-eval-after-load 'eglot
    (add-to-list 'eglot-server-programs
                 '(yaml-ts-mode . ("yaml-language-server" "--stdio"))))

  (add-hook 'yaml-ts-mode-hook #'eglot-ensure)
  (add-hook 'yaml-ts-mode-hook
            (lambda ()
              (setq-local tab-width 2
                          yaml-indent-offset 2)))
#+end_src
*** C programming

New modern C setup!

Eglot for LSP-driven completion and diagnostics. =C-c f= aligns the
region. =;;= chord jumps to end-of-line and inserts a semicolon.
=define-key= and =key-chord-define= mutate the mode-map, so they only
need to run once — use =with-eval-after-load= instead of a hook.

#+begin_src emacs-lisp
  (add-hook 'c-mode-hook #'eglot-ensure)
  (add-hook 'c++-mode-hook #'eglot-ensure)

  (with-eval-after-load 'cc-mode
    (define-key c-mode-map   (kbd "C-c f") #'align-current)
    (define-key c++-mode-map (kbd "C-c f") #'align-current)
    (key-chord-define c-mode-map   ";;" "\C-e;")
    (key-chord-define c++-mode-map ";;" "\C-e;"))
#+end_src

*** CUDA
#+begin_src emacs-lisp
  (use-package cuda-mode
    :straight t
    :init (progn
      (add-to-list 'auto-mode-alist '("\\.cuh\\'" . cuda-mode))))

  (add-hook 'cuda-mode-hook #'eglot-ensure)
#+end_src

*** PlantUML

Locate the =plantuml= binary on PATH (works on macOS Homebrew at
=/opt/homebrew/bin=, Linux distros at =/usr/bin=, manual installs in
=~/.local/bin=, etc.). Falls back to the bare command name so
plantuml-mode can still error usefully if it's not installed.

#+begin_src emacs-lisp
  (use-package plantuml-mode
    :straight t
    :init
    (setq plantuml-executable-path (or (executable-find "plantuml")
                                       "plantuml")
          plantuml-default-exec-mode 'executable))
#+end_src

*** Lua
#+begin_src emacs-lisp
  (use-package lua-mode
    :straight t
    :config (add-to-list 'auto-mode-alist '("\\.lua$" . lua-mode)))
#+end_src

* Back Matter

They say you need this:

#+begin_src emacs-lisp
  (provide 'dot-emacs)
  ;;; dot-emacs ends here
#+end_src
